#include <iostream>
using namespace std;

int main()
	{
	int x,n;
	cin >> x >> n;
	int num[x];
	for(int i=0;i<x;i++)
		{
			cin >> num[i];
		}
	int k;
	for(k=x-n;k<x;k++)
		{
			cout << num[k] << " ";
		}
	int j;
	for(j=0;j<x-n;j++)
		{
			cout << num[j] << " ";
		}
    return 0;    
	}
