#include <iostream>
#include <iomanip>
using namespace std;
int main()
{
int n;
float m;
cin >> n;
cin >> m;
float bmi;
bmi = n / (m*m);
cout << setprecision(2) << fixed << bmi << endl;
if(bmi < 18.5)
    {
        cout << "Underweight";
    }
else if(bmi >= 18.5 && bmi < 25)
    {
        cout << "Normal";
    }
else if(bmi >= 25 && bmi < 30)
    {
        cout << "Overweight";
    }
else if(bmi >= 30)
    {
        cout << "Obese";
    }
return 0;
    
}
